export interface CardType {
  id: number;
  title: string;
  description: string;
  url: string;
}

export interface TestimonialType {
  id: number;
  avatarUrl: string;
  name: string;
  quote: string;
}

export interface QuestionData {
  title: string;
  description: string;
}
